[package]
name = "pearl-process-supervisor"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "PEARL process supervision: spawn, timeout, cancel and whole-tree cleanup (Constitution Article 9)."

[dependencies]
pearl-core.workspace = true
chrono.workspace = true
thiserror.workspace = true
tracing.workspace = true

[target.'cfg(unix)'.dependencies]
nix.workspace = true

[target.'cfg(windows)'.dependencies]
windows-sys.workspace = true

[dev-dependencies]
tempfile.workspace = true

# The Windows contract test must answer "is this pid alive?" without asking the code
# under test, so it needs its own win32 access.
[target.'cfg(windows)'.dev-dependencies]
windows-sys.workspace = true
